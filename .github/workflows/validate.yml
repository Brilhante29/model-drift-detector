name: validate

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Validate repository contract
        shell: pwsh
        run: ./tools/validate-project.ps1 -SkipDocker -AllowPendingEvidence

      - name: Build immutable-path image
        run: docker build -t model-drift-detector .

      - name: Lint
        run: docker run --rm --entrypoint python model-drift-detector -m ruff check src tests tools/aggregate_results.py

      - name: Test with coverage
        run: docker run --rm --entrypoint python model-drift-detector -m pytest --cov=model_drift --cov-report=term-missing --cov-fail-under=90

      - name: Run short container benchmark
        run: |
          mkdir -p benchmarks/results
          docker run --rm --user "$(id -u):$(id -g)"             -v "${{ github.workspace }}/benchmarks/results:/app/benchmarks/results"             model-drift-detector benchmark --rows 400             --output /app/benchmarks/results/ci.json

      - name: Validate benchmark JSON
        run: python -m json.tool benchmarks/results/ci.json > /dev/null

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: model-drift-detector-ci-benchmark
          path: benchmarks/results/ci.json
          if-no-files-found: error
